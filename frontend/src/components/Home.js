import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h4">Bem-vindo ao MedTrace</Typography>
      <Typography variant="body1">Esta é a página inicial do dashboard.</Typography>
    </div>
  );
};

export default Home;
